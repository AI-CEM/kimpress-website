{
  "name": "kimpress-website",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "verify": "node verify-blog.js",
    "build": "node verify-blog.js && node generate-sitemap.js && vite build && node build-static-blog.js",
    "preview": "vite preview"
  },
  "devDependencies": {
    "vite": "^8.0.1"
  },
  "dependencies": {
    "gsap": "^3.14.2"
  }
}
